# User first-10s bugs — Tausch device (apk-v0.1.15)

Screenshots: `tmp-qa/ux-pe-2026-09-24/tausch-user/Tausch/` (5× JPG, 2026-09-24 ~15:41–15:46).

| # | Screenshot | Symptom | Root cause (code) | Fix (0.1.16) |
|---|------------|---------|-------------------|--------------|
| 1 | `154128`, `154152`, `154651` | Lesezeichen reagiert nicht / kein sichtbarer Persistenz-Effekt | Optimistic toggle ohne Rollback bei DB-Fehler; instabile Bookmark-IDs (`Date.now()`); enge Hit-Targets | `app/lesson/[id].tsx`: optimistic + rollback + Alert; `src/data/bookmarks.ts`: stabile ID `lessonId-block-N`; größeres hitSlop |
| 2 | `154423` | Start zeigt nur EmptyState trotz Mini-Player / Lernen | `index.tsx` `hasAnything = continueCard \|\| nextLessonId` — bei Race vor Content-Hydrate oder leerem Manifest beides null; kein Player-Fallback | `index.tsx` + `startData.ts` + `listLessons.ts`: Curriculum aus ContentProvider, Player-Continue, Fallback-Dashboard, EmptyState nur ohne Bundled-Module |
| 3 | `154128`, `154152`, `154651` | Sticky Hören/Quiz überlappt Text; Fortschrittslinie „schwebt“ | `miniPlayerLayoutHeight` zählte 3px statt 44px Scrubber-Hit-Area; Lesson-Scroll-Padding zu klein | `miniPlayerLayout.ts`, `PlaybackScrubber.tsx`, `lessonStickyChrome.ts`, `lesson/[id].tsx` |
| 4 | `154403` | Playlist „Speichern fehlgeschlagen“ | Geräte-DB mit `schema_version` ohne Migration-2-Tabellen (`playlists`); `listPlaylists` [] ok, `INSERT` scheitert | `sqliteDatabase.ts`: `repairPlaylistTables` on init + `withPlaylistTable` für alle Playlist-Ops |

## Emulator repro (emulator-5560, fresh install v0.1.15)

- Start: nach Onboarding + Lektion hören → Start weiterhin „Willkommen / Noch kein Fortschritt“ (EmptyState) bei aktivem Mini-Player.
- Hören → Playlist anlegen → roter Fehler „Speichern fehlgeschlagen“ (wie Screenshot).
- Lektion → Lesezeichen-Icon bleibt outline; Ich → Lesezeichen leer.

## Post-fix smoke

See `SMOKE-FIRST10.md` and `run-smoke-first10.ps1`.
